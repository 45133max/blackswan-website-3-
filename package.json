{
  "name": "blackswan-website-redesign",
  "private": true,
  "scripts": {
    "build": "node scripts/build-deploy.mjs",
    "verify": "node scripts/verify-html.mjs deploy \"deploy/de/index.html\" index.html \"BlackSwan Website.dc.html\"",
    "predeploy": "npm run build",
    "deploy:preview": "npm run build && netlify deploy --dir=deploy",
    "deploy:prod": "npm run build && netlify deploy --dir=deploy --prod"
  }
}
